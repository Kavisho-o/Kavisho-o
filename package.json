{
  "name": "leetcode-pink-viz",
  "version": "1.0.0",
  "private": true,
  "description": "Custom, on-brand LeetCode visualizations (contest curve + spiral heatmap) generated as SVG for a GitHub README.",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-all.mjs",
    "generate:contest": "node scripts/generate-contest-chart.mjs",
    "generate:heatmap": "node scripts/generate-heatmap.mjs",
    "generate:summary": "node scripts/generate-summary-card.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {}
}
